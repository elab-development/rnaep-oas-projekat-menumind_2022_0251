{
  "name": "menumind-auth-service",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MenuMind Auth Service - owns users, sessions and authentication (better-auth). Publishes user-created events.",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts",
    "test": "node --test test/",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "better-auth": "^1.4.18",
    "express": "^4.21.2",
    "kafkajs": "^2.2.4",
    "pg": "^8.13.1",
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.10",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
